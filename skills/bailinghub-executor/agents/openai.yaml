interface:
  display_name: "BailingHub Agent Governance Executor"
  short_description: "Run BailingHub tasks with an OpenClaw executor"
  default_prompt: "Use $bailinghub-executor to connect this OpenClaw agent to a self-hosted BailingHub target as an outbound executor, then verify the governed task path without exposing credentials."
